# TASK_QUEUE

- Add structured audit-event helpers so callers can log allows and denies consistently.
- Add reference policy packs for multi-tenant SaaS and approval workflows.
- Add optional capability- or scope-oriented primitives so the engine can move beyond role-heavy usage.
- Publish CI to run unit tests and package checks on every push.
